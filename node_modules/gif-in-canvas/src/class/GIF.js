"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.GIF = void 0;
const gif_encoder_2_1 = __importDefault(require("gif-encoder-2"));
class GIF {
    width;
    height;
    encoder;
    constructor(width, height) {
        this.width = width;
        this.height = height;
        this.encoder = null;
    }
    create(repeat, delay) {
        const encoder = new gif_encoder_2_1.default(this.width, this.height);
        encoder.start();
        encoder.setRepeat(repeat ? 0 : -1);
        encoder.setDelay(delay);
        this.encoder = encoder;
    }
    addFrame(image) {
        this.encoder.addFrame(image);
    }
    finish() {
        this.encoder.finish();
        return this.encoder.out.getData();
    }
}
exports.GIF = GIF;
